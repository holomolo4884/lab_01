{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build lab_01",
            "type": "shell",
            "command": "cd ${workspaceFolder} && rm -rf build && mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}